/**
 * State Protocol types for the UI.
 *
 * Single source of truth is the in-repo TypeScript binding under
 * `bindings/typescript`. Re-exported here so UI code can `import { ... } from
 * "../protocol/types"` without reaching across the tree.
 */

export * from "../../bindings/typescript/src/index";
